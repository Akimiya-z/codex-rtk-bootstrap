# RTK for Codex App

Always prefer `rtk` for supported shell commands so command output stays compact.

Examples:

```bash
rtk git status
rtk ls
rtk cargo test
rtk rg "pattern" .
```

Use `rtk proxy <command>` when you need raw passthrough output.

Use `rtk gain` to inspect token savings.
